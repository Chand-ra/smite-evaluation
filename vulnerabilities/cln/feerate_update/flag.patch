diff --git a/lightningd/channel_control.c b/lightningd/channel_control.c
index 7018e6345..3b0c9c9ca 100644
--- a/lightningd/channel_control.c
+++ b/lightningd/channel_control.c
@@ -29,6 +29,7 @@
 #include <lightningd/peer_htlcs.h>
 #include <wally_bip32.h>
 #include <wally_psbt.h>
+#include <stdio.h>
 
 struct splice_command {
 	/* Inside struct lightningd splice_commands. */
@@ -93,6 +94,10 @@ static void try_update_feerates(struct lightningd *ld, struct channel *channel)
 	if (!channel->owner)
 		return;
 
+	if (!streq(channel->owner->name, "channeld")) {
+		fprintf(stderr, "SMITE_FLAG_FEERATE_UPDATE\n");
+	}
+
 	channel_update_feerates(ld, channel);
 }
 
