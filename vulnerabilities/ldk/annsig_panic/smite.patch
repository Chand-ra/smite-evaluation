diff --git a/workloads/ldk/Cargo.toml b/workloads/ldk/Cargo.toml
index 14ddbb1..356562f 100644
--- a/workloads/ldk/Cargo.toml
+++ b/workloads/ldk/Cargo.toml
@@ -9,5 +9,5 @@ edition = "2024"
 panic = "abort"
 
 [dependencies]
-ldk-node = "0.7"
+ldk-node = { git = "https://github.com/lightningdevkit/ldk-node", rev = "6be230ec01e852491fff078613b095278452cbc7" }
 libc = "0.2"
diff --git a/workloads/ldk/src/main.rs b/workloads/ldk/src/main.rs
index 77696ba..e98e575 100644
--- a/workloads/ldk/src/main.rs
+++ b/workloads/ldk/src/main.rs
@@ -9,6 +9,7 @@ use std::sync::atomic::{AtomicBool, Ordering};
 use std::time::Duration;
 
 use ldk_node::Builder;
+use ldk_node::Config;
 use ldk_node::bitcoin::Network;
 
 /// Signal handler sets this to true to trigger shutdown.
@@ -45,7 +46,9 @@ fn main() {
         .parse()
         .expect("valid listen address");
 
-    let node = Builder::new()
+    let mut config = Config::default();
+    config.manually_accept_inbound_channels = true;
+    let node = Builder::from_config(config)
         .set_network(Network::Regtest)
         .set_chain_source_bitcoind_rpc(
             "127.0.0.1".to_string(),
