diff --git a/workloads/ldk/Cargo.toml b/workloads/ldk/Cargo.toml
index 14ddbb1..edcc3e7 100644
--- a/workloads/ldk/Cargo.toml
+++ b/workloads/ldk/Cargo.toml
@@ -9,5 +9,5 @@ edition = "2024"
 panic = "abort"
 
 [dependencies]
-ldk-node = "0.7"
+ldk-node = { git = "https://github.com/lightningdevkit/ldk-node", rev = "34577b66d4a270e179d1dca5f252b22dfeeb6cd1" }
 libc = "0.2"
diff --git a/workloads/ldk/src/main.rs b/workloads/ldk/src/main.rs
index 77696ba..7eff7d6 100644
--- a/workloads/ldk/src/main.rs
+++ b/workloads/ldk/src/main.rs
@@ -6,6 +6,7 @@
 
 use std::net::SocketAddr;
 use std::sync::atomic::{AtomicBool, Ordering};
+use ldk_node::entropy::{generate_entropy_mnemonic, NodeEntropy};
 use std::time::Duration;
 
 use ldk_node::Builder;
@@ -45,6 +46,9 @@ fn main() {
         .parse()
         .expect("valid listen address");
 
+    let mnemonic = generate_entropy_mnemonic(None);
+	let node_entropy = NodeEntropy::from_bip39_mnemonic(mnemonic, None);
+
     let node = Builder::new()
         .set_network(Network::Regtest)
         .set_chain_source_bitcoind_rpc(
@@ -56,7 +60,7 @@ fn main() {
         .set_listening_addresses(vec![listen_addr.into()])
         .expect("valid listening addresses")
         .set_storage_dir_path(data_dir)
-        .build()
+        .build(node_entropy)
         .expect("node build");
 
     node.start().expect("node start");
